-- SELECT COUNT(ticketPrice) FROM flights;

-- SELECT MIN(ticketPrice), MAX(ticketPrice) FROM flights;

-- SELECT MIN(flightDate), MAX(flightDate) FROM flights;

-- SELECT SUM(ticketPrice) FROM flights;

-- SELECT AVG(ticketPrice) FROM flights;

